const fs = require("fs"); const envPath = "../.env"; try { const content = fs.readFileSync(envPath, "utf8"); console.log("Found .env file!"); const lines = content.split("\n"); const mongoLine = lines.find(line => line.startsWith("MONGO_URI=")); if (mongoLine) { const uri = mongoLine.substring(10); console.log("Found MONGO_URI: " + uri.substring(0, 20) + "..."); } else { console.log("MONGO_URI not found in .env"); } } catch (e) { console.error("Error:", e.message); }
