const { MongoClient } = require("mongodb"); console.log("Test successful"); async function main() { try { console.log("Trying to connect to MongoDB..."); const mongoUrl = process.env.MONGO_URI; if (!mongoUrl) throw new Error("MONGO_URI not set"); console.log("MongoDB URL found:", mongoUrl.substring(0, 15) + "..."); const client = new MongoClient(mongoUrl); await client.connect(); console.log("Connected to MongoDB!"); await client.close(); } catch(e) { console.error("Error:", e); } } main();
