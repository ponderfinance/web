/** @type {import('reshaped').ReshapedConfig} */
const config = {
  themes: {
    ponder: {
      color: {
        backgroundPage: { hex: '#131313' },
      },
      viewport: {
        m: { minPx: 660 },
        l: { minPx: 900 },
        xl: { minPx: 1280 },
        s: { maxPx: 659 },
      },
    },
  },
}

module.exports = config
